import React from 'react';
import { ApplicationData } from './types';

const DocumentsTab: React.FC<{ applicationData: ApplicationData }> = ({ applicationData }) => (
  <div>
    {/* ...existing documents tab JSX... */}
  </div>
);

export default DocumentsTab;
