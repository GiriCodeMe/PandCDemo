import React from 'react';
import { ApplicationData } from './types';

const RequirementsTab: React.FC<{ applicationData: ApplicationData }> = ({ applicationData }) => (
  <div className="space-y-4">
    {/* ...existing requirements tab JSX... */}
  </div>
);

export default RequirementsTab;
